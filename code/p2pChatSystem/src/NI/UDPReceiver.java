package NI;

public class UDPReceiver extends AbstractReceiver {

	// On a comme champ le singleton de la classe
	private UDPReceiver singleton;
	
	
	/*
	 * On définit les constructeurs
	 */
	public UDPReceiver
}
